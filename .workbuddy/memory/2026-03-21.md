# 2026-03-21

## 字体替换为霞鹜新晰黑 Plus

- 将根目录 `LXGWNeoZhiSongPlus.ttf` 复制到 `assets/` 和 `static/`
- 创建 `assets/css/custom.css`，使用 `@font-face` 引入字体，设置全局 `font-family`（代码块保留等宽字体）
- Blowfish 主题自动识别 `assets/css/custom.css` 并打包进 CSS bundle，无需在 params.toml 单独注册
- 验证：`public/css/main.bundle.min.*.css` 中包含 `LXGWNeo` 字样，字体注入成功
- 字体路径改为相对路径 `../LXGWNeoZhiSongPlus.ttf`（CSS 打包后位于 `public/css/`，字体在 `public/`，相对路径正确）
